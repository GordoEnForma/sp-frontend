import { Typography } from "@mui/material"
import { StudentLayout } from "../layout/StudentLayout"

export const StudentResourcesPage = () => {
    return (
        <StudentLayout>
            <Typography>Página de los Recursos</Typography>
        </StudentLayout>
    )
}
