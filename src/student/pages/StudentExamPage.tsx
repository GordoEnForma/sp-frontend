import { Typography } from "@mui/material"
import { StudentLayout } from "../layout/StudentLayout"

export const StudentExamPage = () => {
    return (
        <StudentLayout>
            <Typography>Página del Examen</Typography>
        </StudentLayout>
    )
}
