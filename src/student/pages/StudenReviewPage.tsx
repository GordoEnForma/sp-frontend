import { Typography } from "@mui/material"
import { StudentLayout } from "../layout/StudentLayout"

export const StudentReviewPage = () => {
    return (
        <StudentLayout>
            <Typography >Página del Repaso</Typography>
        </StudentLayout>
    )
}
