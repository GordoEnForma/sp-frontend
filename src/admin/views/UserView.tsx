import { UserForm } from "../components";

export const UserView = () => {
    return (
        <>
            {/*  UserForm -> Add Users */}
            <UserForm />

            {/* UserTable -> Check users and open UserDetailsModal */}
        </>
    );
};
